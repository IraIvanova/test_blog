{% extends '@Admin/base.html.twig' %}

{% block body %}


    <!-- Post Content -->
    <article>
        <div class="container">
            <div class="row">
                <div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">
                    <h1>{{ blog.title }}</h1>

                    <img src="/images/{{ blog.image }}" style="width:600px">

                    <div>
                    {{ blog.description }}
                    </div>
                </div>
            </div>
        </div>
    </article>

    <hr>

    <a href="{{ path('blog_admin.homepage') }}"><< Back to list</a>
{% endblock %}

