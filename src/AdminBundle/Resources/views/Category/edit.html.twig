{% extends '@Admin/base.html.twig' %}

{% block body %}
    <h3>Category edit:</h3>
    <form method="post" action="{{ path('category.edit', {id:category.id} ) }}" >
        {{ form_widget(form) }}
        <input type="submit" />
    </form>
{% endblock %}